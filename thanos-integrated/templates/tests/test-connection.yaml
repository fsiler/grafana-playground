apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thanos-integrated.fullname" . }}-test-connection"
  labels:
    {{- include "thanos-integrated.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thanos-integrated.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
